distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.14-all.zip
# Without this line the wrapper downloads the Gradle distribution unverified
# on every CI run and executes it - in the same job that holds the signing
# key (audit NEU-7). With it, the wrapper refuses to start unless the archive
# matches bit for bit.
# Source: https://gradle.org/release-checksums/ (Complete "-all" ZIP, 8.14)
# When raising the Gradle version this hash MUST be updated with it,
# otherwise every Android build fails.
distributionSha256Sum=efe9a3d147d948d7528a9887fa35abcf24ca1a43ad06439996490f77569b02d1
