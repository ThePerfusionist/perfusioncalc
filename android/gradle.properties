org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
# Opt-outs written by the Flutter migrator during the AGP 8.11 upgrade
# (2026-07). They are debt with an expiry date: Flutter will eventually make
# the new Gradle DSL and the built-in Kotlin support the only path. Revisit
# when the AGP/Flutter versions are next raised - if the build works without
# them, delete both lines.
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
