// Stub für Mobile/Desktop. Wird nie aufgerufen, weil die Buttons nur auf
// Web aktiv sind, aber existiert, damit der conditional import kompiliert.

void launchExternal(String relativeUrl) {
  // No-op
}
